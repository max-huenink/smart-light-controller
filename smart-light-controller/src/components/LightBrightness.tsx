import { useState, useEffect, useCallback } from 'react';
import BrightnessTrait from '../models/traits/BrightnessTrait';

export default function LightBrightness({ Brightness }: BrightnessTrait) {
  return (<div>
    <h1>This is brightness!</h1>
  </div>);
}